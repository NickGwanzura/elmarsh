import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { PageHero } from "@/components/PageHero";
import { services, pageMetadata } from "@/lib/site";
export const metadata: Metadata = pageMetadata("Services", "Explore vehicle shipping, storage, loading, port delivery and cargo handling services from Elmarsh Logistics in Huntingdon, UK.");
export default function Services(){return <><PageHero eyebrow="Our services" title="The right support for every stage" description="Vehicle logistics and flexible cargo handling for customers across the UK." image="https://images.unsplash.com/photo-1586528116311-ad8dd3c8310d?auto=format&fit=crop&w=1800&q=85"/><section className="section"><div className="container"><div className="section-heading"><span className="eyebrow">Full capability</span><h2>What can we help you move?</h2><p>Every requirement starts with the details. Share the item, dimensions, collection point and destination so our team can recommend the appropriate service.</p></div><div className="service-grid">{services.map(({title,text,icon:Icon})=><article className="service-card" key={title}><div className="icon-box"><Icon/></div><h3>{title}</h3><p>{text}</p><Link className="text-link" href="/request-a-quote">Get a quote <ArrowRight size={14}/></Link></article>)}</div><div className="notice">Liquids, chemicals and regulated materials must be declared and confirmed with our team before delivery.</div></div></section></>}
