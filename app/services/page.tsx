import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { PageHero } from "@/components/PageHero";
import { services, pageMetadata } from "@/lib/site";
export const metadata: Metadata = pageMetadata("Services", "Explore truck sales, vehicle shipping, collection, delivery, storage, loading and cargo handling from Elmarsh Logistics in Huntingdon, UK.");
export default function Services(){return <><PageHero eyebrow="Our services" title="The right support for every stage" description="Truck sales, vehicle logistics and flexible cargo handling — all coordinated in one place." image="https://images.unsplash.com/photo-1586528116311-ad8dd3c8310d?auto=format&fit=crop&w=1800&q=85"/><section className="section"><div className="container"><div className="section-heading"><span className="eyebrow">Full capability</span><h2>One stop. Every logistics stage.</h2><p>From sourcing a truck to collecting, loading, storing and delivering it, our team keeps the full journey clear and controlled.</p></div><div className="service-grid">{services.map(({title,text,icon:Icon})=><article className="service-card" key={title}><div className="icon-box"><Icon/></div><h3>{title}</h3><p>{text}</p><Link className="text-link" href="/request-a-quote">Get a quote <ArrowRight size={14}/></Link></article>)}</div><div className="notice">Liquids, chemicals and regulated materials must be declared and confirmed with our team before delivery.</div></div></section></>}
