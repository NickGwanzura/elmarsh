services:
  elmarsh:
    build:
      context: .
      dockerfile: Dockerfile
    image: elmarsh-logistics:latest
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-http://localhost:3000}
    restart: unless-stopped
